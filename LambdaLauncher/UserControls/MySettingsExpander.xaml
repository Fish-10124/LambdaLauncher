<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="LambdaLauncher.UserControls.MySettingsExpander"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">

    <Grid>
        <controls:SettingsExpander x:Name="rootExpander" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                   IsExpanded="{x:Bind IsExpanded, Mode=OneWay}">
            <controls:SettingsExpander.Header>
                <Grid x:Name="SettingsExpanderHeader" ColumnSpacing="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource BorderIconStyle}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Icon}" />
                        </Border.Background>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind Header, Mode=OneWay}" />

                        <TextBlock Grid.Row="1"
                                   Foreground="{ThemeResource DescriptionTextForegroundBrush}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Text="{x:Bind Description, Mode=OneWay}" />
                    </Grid>
                </Grid>
            </controls:SettingsExpander.Header>
        </controls:SettingsExpander>
    </Grid>
</UserControl>