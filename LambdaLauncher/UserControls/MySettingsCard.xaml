<?xml version="1.0" encoding="utf-8" ?>
<UserControl x:Class="LambdaLauncher.UserControls.MySettingsCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="using:CommunityToolkit.WinUI.Controls"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d">

    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="WindowWidthStates">
                <VisualState x:Name="NarrowWidth">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="iconBorder.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="WideWidth">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="670" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="iconBorder.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <controls:SettingsCard x:Name="rootCard" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               Click="rootCard_Click"
                               Command="{x:Bind Command, Mode=OneWay}"
                               CommandParameter="{x:Bind CommandParameter, Mode=OneWay}"
                               Content="{x:Bind Content, Mode=OneWay}"
                               IsClickEnabled="True"
                               Tag="{x:Bind Tag, Mode=OneWay}">
            <controls:SettingsCard.Header>
                <Grid x:Name="SettingsCardHeader" ColumnSpacing="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="iconColumn" Width="Auto" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="iconBorder" Grid.Column="0" Style="{StaticResource BorderIconStyle}">
                        <Border.Background>
                            <ImageBrush ImageSource="{x:Bind Icon, Mode=OneWay}" />
                        </Border.Background>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind Header, Mode=OneWay}" />

                        <TextBlock Grid.Row="1"
                                   Foreground="{ThemeResource DescriptionTextForegroundBrush}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Text="{x:Bind Description, Mode=OneWay}"
                                   TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                    </Grid>
                </Grid>
            </controls:SettingsCard.Header>
        </controls:SettingsCard>
    </Grid>
</UserControl>