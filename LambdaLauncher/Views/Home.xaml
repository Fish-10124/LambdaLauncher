<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="LambdaLauncher.Views.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid>
        <Button x:Name="launchButton" Width="250" Height="60"
                Margin="50" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock x:Uid="launchButton" Grid.Row="0" Style="{ThemeResource BodyTextBlockStyle}" />
                <TextBlock x:Uid="launchButtonDetails" Grid.Row="1" FontSize="11"
                           Foreground="{ThemeResource DescriptionTextForegroundBrush}"
                           Style="{ThemeResource CaptionTextBlockStyle}" />
            </Grid>
        </Button>
    </Grid>
</Page>